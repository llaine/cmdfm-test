'use strict';


const jukeboxReducer = (state = [], action) => {
  switch (action.type) {
    case 'DISPLAY_STYLES':
      break;
    case 'PLAY':

      break;
    default:
      return state;
  }
};